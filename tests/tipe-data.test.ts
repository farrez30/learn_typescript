describe('Data Type', () => {
    it('should must declare', () => {
        let name: string = "Ayam"
        let balance: number = 100000
        let isVip: boolean = true

        console.info(name);
        console.info(balance);
        console.info(isVip)

        // name = 100;
        // balance = "eko";
        // isVip = 100;
    });
});