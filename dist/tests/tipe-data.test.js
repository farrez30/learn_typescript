"use strict";
describe('Data Type', () => {
    it('should must declare', () => {
        let name = "Ayam";
        let balance = 100000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
        // name = 100;
        // balance = "eko";
        // isVip = 100;
    });
});
